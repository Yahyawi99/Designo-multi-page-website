import React from "react";
// components
import Header from "../components/Header";

const Home = () => {
  return (
    <section className="home">
      <Header />
    </section>
  );
};

export default Home;
